const categories = document.querySelectorAll('.item');
console.log('Number of categories:', categories.length);

const itemCategory = categories.forEach(function(category) {s
    return console.log(category);
})

